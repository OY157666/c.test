#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>


int main()
{
	char str[100];
	scanf("%s", str);
	printf("%s\n", str);
	char* p = str;
	while (1)
	{
		char c = *p;
		if (c == '\0')
			break;
		printf("%c\n", c);
		p++;
	}

	return 0;
}
